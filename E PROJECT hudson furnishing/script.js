function addToCart1() {
    var button = document.getElementById("addToCartBtn(1)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart2() {
    var button = document.getElementById("addToCartBtn(2)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart3() {
    var button = document.getElementById("addToCartBtn(3)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart4() {
    var button = document.getElementById("addToCartBtn(4)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart5() {
    var button = document.getElementById("addToCartBtn(5)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart6() {
    var button = document.getElementById("addToCartBtn(6)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart7() {
    var button = document.getElementById("addToCartBtn(7)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart8() {
    var button = document.getElementById("addToCartBtn(8)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function addToCart9() {
    var button = document.getElementById("addToCartBtn(9)");
    
    // Toggle the button text
    if (button.innerHTML === "Add to Cart") {
      button.innerHTML = "Added Successfully";
    } else {
      button.innerHTML = "Add to Cart";
    }
  }
  function scrollToSection() {
    var section = document.getElementById("section");
    section.scrollIntoView({ behavior: 'smooth' });
  }